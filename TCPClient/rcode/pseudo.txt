

int match_name(* nameOfSubject, "hansi*")
int match_user(* nameOfSubject, "root")





/* testing */

for i in argc
  switch (argv[i])
    case "-name"
      i++
      value = argv[i]
      if (! test_name(dirName, value)) break;

    case
      "-user"
      i++
      if (!test_user(dirname, argv[i])) break;





case "-ls"
  enable_output
case "-print"
  do_more_output


    -name <pattern>
    -ls
    -print
    -user <uid>

    -nouser: errno prüfen, damit zwischen Fehler und "nicht gefunden" unterschieden werden kann

argv ist ein array von char pointern, wobei der letzte Pointer auf NULL zeigt
